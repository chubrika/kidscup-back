# Server
PORT=3000
NODE_ENV=development

# MongoDB (replace <db_password> with your Atlas password; if it has @ # $ % etc., URL-encode it)
MONGODB_URI=mongodb+srv://chubro15_db_user:<db_password>@kidscup.ykzzzon.mongodb.net/kidscup

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (comma-separated origins, or * for all)
# e.g. http://localhost:3000 (frontend), http://localhost:3001 (Next.js), http://localhost:4200 (admin)
CORS_ORIGIN=http://localhost:4200,http://localhost:3000,http://localhost:3001
